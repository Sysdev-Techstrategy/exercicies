// ## isGreaterThan(a, b)

// Returns true if `a` is greater than `b`

// **Parameters**

// **a**: number

// **b**: number

// **Returns**: boolean, `a` is larger than `b`

function isGreaterThan(a, b) {
  let result = a > b ? true : false;
  return result;
}
console.log(isGreaterThan(2, 10))